version: '2'
services:
  mashaghelapplication-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/MashaghelApplication/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=MashaghelApplication
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
